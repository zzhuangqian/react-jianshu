import * as constants from './constants'

const defaultState = {
    title:'sdfsdfs',
    content:'sdfsdfsdfsdf'
}

export default (state = defaultState,action) => {
    switch(action.type){
        default : 
        return state
    }
}